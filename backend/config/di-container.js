// Import the BottleJS library for dependency injection
const Bottle = require("bottlejs");

// Create a new Bottle instance
const bottle = new Bottle();

// Export the Bottle instance for use in other parts of the application
module.exports = bottle;
